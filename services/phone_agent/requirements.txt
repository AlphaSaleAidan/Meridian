# Meridian AI Phone Agent — Pipecat 1.x pipeline dependencies (rebuilt).
# Commercial-friendly (MIT / Apache-2.0 / BSD). Heavy (torch via STT/TTS) — runs on
# a capable host, NOT the lean Railway API image (kept as a sidecar).

# Voice pipeline (1.x). Extras pull the built-in service deps we use:
#   silero  → VAD            moonshine → STT (EN, Phase 1)
#   kokoro  → TTS (Phase 1)  deepseek  → LLM (OpenAI-compatible)
#   nvidia  → Nemotron ASR/TTS + LLM (Phase 2)
#   websocket → FastAPI websocket transport (Telnyx/Twilio media streams)
pipecat-ai[silero,moonshine,kokoro,deepseek,nvidia,websocket]==1.4.0

# Web framework (sidecar HTTP/WS entrypoint)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# HTTP client for POS APIs + Supabase logging
httpx>=0.27.0

# Utilities
numpy>=1.26.0
python-dotenv>=1.0.0
