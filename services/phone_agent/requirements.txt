# Meridian AI Phone Agent — Pipecat pipeline dependencies
# All packages have commercial-friendly licenses (MIT / Apache-2.0 / BSD).

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Voice pipeline — needs Twilio serializer (added in 0.0.40+)
pipecat-ai>=0.0.45

# STT — Moonshine (English, CPU-realtime, ~107ms latency)
useful-moonshine>=0.1.0

# STT fallback — Whisper for non-English
whisperlivekit>=0.3.0
faster-whisper>=1.0.0

# TTS — Kokoro-82M (Apache-2.0, ~300 MB, top of TTS-Arena Jan 2026)
kokoro>=0.9.0
soundfile>=0.12.0

# TTS voice cloning (optional, only if merchant uploads a reference sample)
# cosyvoice>=0.2.0

# LLM — SambaNova via OpenAI-compatible API + Ollama fallback
aiohttp>=3.9.0

# Audio processing
numpy>=1.26.0
scipy>=1.12.0

# VAD — Silero (MIT, <1ms per chunk on CPU)
silero-vad>=5.0

# HTTP client for POS APIs + Supabase
httpx>=0.25.0

# Utilities
python-dotenv>=1.0.0
