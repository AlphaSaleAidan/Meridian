# meridian local agent — CPU-only vision on the merchant's own PC/POS.
# No GPU required (YOLO11n runs on CPU at a few fps, plenty for footfall/dwell).
httpx>=0.27.0
numpy>=1.26
opencv-python-headless>=4.9.0
ultralytics>=8.1.0
supervision>=0.21.0
