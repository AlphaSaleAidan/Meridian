__pycache__/
*.pyc
# Env files — never commit real secrets. Only *.env.example templates are tracked.
.env
.env.*
!.env.example
**/.env
**/.env.local
**/.env.*.local
*.zip
*.pdf
.DS_Store
node_modules/
logs/
tools/update_supa_key.sh
services/deerflow/
services/training_video_pipeline/output/
services/training_video_pipeline/.env
services/mike/
.vercel
data/models/
data/vectors.db
data/spaces/
data/scraped/
data/swarm_traces.sqlite
celerybeat-schedule
supabase/.temp/
.claude-flow/sessions/
.claude-flow/data/
frontend/.claude-flow/sessions/
frontend/.claude-flow/data/
services/postal/.claude/
.claude/settings.json

# Model weights
*.pt

frontend/src/lib/.claude-flow/
services/labellerr_cv/

# Catch every nested .claude-flow/ tooling drop wherever it lands
**/.claude-flow/
services/higgsfield/

# local test venv (worktree-only, do not commit)
.venv-test/

# LiteLLM gateway secrets (master key, budget) — never commit
.env.litellm

# Postal mail-server private key — must exist on disk for the containers,
# must never be committed. Canonical copy: /root/.secrets/postal-signing.key
services/postal/signing.key
