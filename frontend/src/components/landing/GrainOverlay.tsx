/**
 * Fixed SVG grain texture overlay at 3% opacity.
 * Uses CSS animation for subtle movement.
 */
export default function GrainOverlay() {
  return <div className="grain-overlay" aria-hidden="true" />
}
