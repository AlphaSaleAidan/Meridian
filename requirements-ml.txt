# All ML packages are lazy-imported — Railway build won't break if these fail.
# Heavy packages (torch-dependent) are commented out to avoid OOM on Railway.

# Vision — Camera Detection (lazy import in src/camera/detector.py)
# ultralytics>=8.4.0    # pulls torch ~2GB — OOM on Railway; the API instead
#                       # runs the bundled yolo11n.onnx via onnxruntime (below)
# supervision installs WITHOUT ultralytics/torch (ByteTrack + PolygonZone only).
# Pin <0.30: ByteTrack is removed in 0.30.
supervision>=0.28.0,<0.30
onnxruntime>=1.17
opencv-python-headless>=4.8.0
lapx>=0.9.0

# Prompt Optimization (lazy import in src/ai/dspy_optimizer.py)
# dspy>=2.5.0           # pulls torch via sentence-transformers

# Agent Memory (lazy import in src/ai/agent_memory.py)
# mem0ai>=0.1.0         # pulls torch + chromadb + onnxruntime

# Pose Estimation (lazy import in src/ai/freemocap/skeletal_tracker.py)
# skellytracker>=0.1.0  # pulls torch + mediapipe

# Forecasting
statsforecast>=2.0.0

# Anomaly Detection (lazy import in src/ai/agents/base.py)
pyod>=2.0.0
luminol>=0.4

# Customer LTV
lifetimes>=0.11.0

# Wave 1B — pymc-marketing is the Bayesian successor to lifetimes (adds
# per-customer credible intervals). Pin <0.12 because 0.12+ requires
# pydantic>=2.10 (uses ``SupportedPrior`` with pydantic schema generation),
# while production currently pins pydantic==2.9.2 in requirements.txt.
# Re-evaluate this cap when the FastAPI app is on pydantic>=2.10.
pymc-marketing>=0.10,<0.12

# Wave 1A — panel price elasticity (OLS + DoubleML).

# Wave 1D — XGBoost/LightGBM + CalibratedClassifierCV for churn classifier.
# (lightgbm is already declared above; xgboost is new.)
xgboost>=2.0
# SHAP — explainability layer used by the churn classifier (Wave 1D + 2B).
shap>=0.45

# Basket Analysis (lazy import in src/ai/agents/base.py)
mlxtend>=0.23

# Forecasting (ML-based)
mlforecast>=0.13.0
lightgbm>=4.0.0

# A/B Testing
bayesabtest>=0.3.0

# Phone agent — Pipecat voice pipeline (only loaded when MEDIA_STREAMS_ENABLED=1)
# Per the Dockerfile pattern, failed installs are tolerated and skipped.
# Pinned to 0.0.x: pipecat 1.0 (Oct 2025) rewrote the service base classes
# (LLMService/STTService/TTSService moved out of pipecat.services.ai_services,
# run_stt is now bytes→AsyncGenerator, etc). Our bot.py targets the 0.0.x API.
# Migration to 1.x is a planned follow-up.
pipecat-ai>=0.0.45,<1.0
silero-vad>=5.0,<6.0
useful-moonshine>=0.1.0
kokoro>=0.9.0
soundfile>=0.12.0
# WhisperLiveKit fallback for non-English calls (still torch-dependent)
whisperlivekit>=0.3.0
faster-whisper>=1.0.0
