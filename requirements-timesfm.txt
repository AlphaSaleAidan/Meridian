# Optional TimesFM LOCAL-inference extra — NOT part of core requirements and NOT
# installed in the Railway API image (torch OOMs Railway — see requirements-ml.txt).
#
# Only needed to run TimesFM in-process (LOCAL mode) on a host with enough RAM.
# The PROD path is ENDPOINT mode: stand up a small sidecar that installs these and
# exposes a /forecast HTTP endpoint, then set TIMESFM_ENDPOINT on the API service —
# that keeps torch out of the lean API container.
#
#   pip install -r requirements-timesfm.txt     # on the sidecar / capable host
#
# Pin/adjust to the torch build that matches the host (CPU vs CUDA).
timesfm[torch]>=2.0.0
