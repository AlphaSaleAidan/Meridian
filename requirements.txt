# Core
# <0.139: under 0.139 app.py's include_router registers 0 of 292 routes
# (silent empty app — first caught by compliance CI 2026-07-12); 0.133.x is
# the proven-good line locally and in prod deploys.
fastapi>=0.115.6,<0.139
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
httpx==0.27.2
stripe>=11.0.0  # unified payments (Stripe Connect) + checkout sessions
python-dotenv==1.0.1
pydantic==2.9.2
email-validator>=2.0.0

# Task Queue
celery>=5.3.0
redis>=5.0.0

# Database
supabase>=2.9.0
asyncpg>=0.30.0

# Security
cryptography>=42.0.0
bcrypt>=4.0.0

# Data
numpy>=1.26,<2
pandas>=2.2
scipy>=1.14
pyyaml>=6.0

# Analytics

# LLM Observability

# Multi-Model LLM Routing
litellm>=1.40.0

# Monitoring & Analytics
sentry-sdk[fastapi]>=2.0.0
posthog>=3.0.0

# AI Generation

# Menu ingestion reads PDF menus — plenty of restaurants have nothing else.
# It was left out deliberately ("don't pull heavy deps"), but pypdf is pure
# Python and about a megabyte, and leaving it out did not make PDFs
# unsupported — it made support ACCIDENTAL: it arrives transitively via
# browser-use in some environments and not in others, so the same upload
# parsed in dev and was flagged pdf_unsupported in production. Pinned here so
# the behaviour is the same everywhere.
pypdf>=5.0.0
